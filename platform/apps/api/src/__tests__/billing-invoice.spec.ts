describe("Billing + invoicing", () => {
  it.todo("generates an invoice with meter reads and utility lines");
  it.todo("applies a late fee only once per overdue invoice");
  it.todo("ties payout reconciliation math to invoice AR ledger entries");
});
