# Changesets

Use Changesets for versioning the shared packages: `@keepr/shared`, `@keepr/sdk`,
and `@keepr/integrations-sdk`. Apps are ignored in `.changeset/config.json`.

## Common commands
- `pnpm changeset`
- `pnpm changeset:status`
- `pnpm changeset:version`
