[build]
builder = "NIXPACKS"

[build.nixpacksOptions]
installCommand = "pnpm install --no-frozen-lockfile"

[deploy]
startCommand = "cd platform/apps/web && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
